# Core backend — installs on every OS. The system runs fully on these.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0

# Agents (cross-platform)
psutil>=6.0
APScheduler>=3.10
dateparser>=1.2
duckduckgo-search>=6.0
httpx>=0.27
beautifulsoup4>=4.12
watchdog>=4.0
aiosqlite>=0.20
pyperclip>=1.9

# Windows control (skipped automatically on non-Windows)
pyautogui>=0.9.54 ; sys_platform == "win32"
pygetwindow>=0.0.9 ; sys_platform == "win32"
pillow>=10.0 ; sys_platform == "win32"
pycaw>=20240210 ; sys_platform == "win32"
comtypes>=1.4 ; sys_platform == "win32"

# NOTE: voice extras (vosk, openai-whisper, sounddevice, pyttsx3, numpy) are
# large and installed separately by setup.bat so the core never fails if they
# don't. Voice is optional — the system runs without it.
